import {CaretDownOutlined} from '@ant-design/icons'
import styled from '@emotion/styled'

const MyIcon = styled(CaretDownOutlined ) `
  font-size: 50px;
`
export default function LibraryIconPage() {
  return <MyIcon />  
}

